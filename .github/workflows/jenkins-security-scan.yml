name: Jenkins Security Scan

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '0 6 * * 1'

jobs:
  security-scan:
    uses: jenkins-infra/jenkins-security-scan/.github/workflows/security-scan.yaml@v2
    with:
      java-version: 17
